# ── Core ──
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0

# ── Database ──
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
psycopg2-binary==2.9.12
alembic==1.19.1

# ── Task Queue ──
celery[redis]==5.6.3
redis==8.1.0

# ── HTTP Client ──
httpx==0.28.1

# ── YouTube upload ──
google-api-python-client==2.199.0
google-auth-oauthlib==1.4.1
google-auth-httplib2==0.4.2

# ── Media ──
# ffmpeg-python==0.2.0  # We use subprocess directly
opencv-python-headless>=5.0.0.93   # cv2 — frame analysis in video_evaluator (headless: no GUI deps)

# ── Utilities ──
python-multipart==0.0.32
python-dotenv==1.2.3
PyYAML>=6.0.3          # LoRA catalog / config parsing
yt-dlp>=2026.8.19       # source-clip download helper

# ── AI / ML (image & video generation) ──
# GPU users: install a CUDA build of torch from the official index, e.g.
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
# A plain `pip install` here yields a CPU build (fine for stub mode).
diffusers>=0.40.0
transformers>=5.16.1
accelerate>=1.14.0
safetensors>=0.8.0
torch>=2.13.0
Pillow>=12.3.0
rembg>=2.0.81
